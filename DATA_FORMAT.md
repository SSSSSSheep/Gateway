# 网关系统数据格式文档

## 一、上行数据格式（设备 → 网关 → 云端）

### 1.1 蓝牙原始数据格式
蓝牙模块接收到的原始数据包格式：

```
| 固定头部 | 长度 | ID | 消息内容 |
| 2字节   | 1字节| 2字节| N字节    |
```

- **固定头部**: `0xf1 0xdd` (2字节)
- **长度**: 消息总长度，包括ID和消息内容 (1字节，最大250)
- **ID**: 设备ID (2字节)
- **消息内容**: 实际数据 (N字节，长度 = 总长度 - 2)

### 1.2 网关内部消息格式
网关内部处理的消息格式：

```
| 连接类型 | ID长度 | 消息长度 | ID | 消息 |
| 1字节    | 1字节  | 1字节    | 2字节| N字节|
```

- **连接类型**: 1 (蓝牙连接)
- **ID长度**: 2 (固定为2字节)
- **消息长度**: 消息内容长度 (N字节)
- **ID**: 设备ID (2字节)
- **消息**: 实际数据 (N字节)

### 1.3 MQTT JSON格式
发送到云端的JSON格式：

```json
{
  "id": "设备ID",
  "msg": "消息内容"
}
```

## 二、下行数据格式（云端 → 网关 → 设备）

### 2.1 MQTT JSON格式
从云端接收的JSON格式：

```json
{
  "id": "设备ID",
  "msg": "消息内容"
}
```

### 2.2 网关内部消息格式
网关内部处理的消息格式（与上行相同）：

```
| 连接类型 | ID长度 | 消息长度 | ID | 消息 |
| 1字节    | 1字节  | 1字节    | 2字节| N字节|
```

### 2.3 蓝牙AT指令格式
发送到蓝牙设备的AT指令格式：

```
AT+MESH<设备ID><消息内容>

```

- `AT+MESH`: 固定前缀 (8字节)
- `<设备ID>`: 2字节设备ID
- `<消息内容>`: N字节消息内容
- `
`: 结束符 (2字节)

## 三、数据转换流程

### 3.1 上行数据流
1. 蓝牙设备发送原始数据（固定头部 + 长度 + ID + 消息）
2. 网关通过串口读取数据
3. `app_bt_postRead` 函数解析数据，转换为内部消息格式
4. 内部消息通过线程池提交MQTT发布任务
5. `send_message_handler` 函数将内部消息转换为JSON格式
6. 通过MQTT发送到云端

### 3.2 下行数据流
1. 云端通过MQTT发送JSON数据
2. recv_msg_callback 接收JSON，转换为内部消息格式（[conn_type][id_len][msg_len][id][msg]）
3. 通过线程池提交串口发送任务（JOB_TYPE_SERIAL_SEND）
4. write_thread_handler 调用 app_bt_preWrite，将内部消息转换为蓝牙AT指令格式
5. 转换后的AT指令写入 write_queue（双缓冲队列），并发送条件变量信号 write_cond
6. write_thread_fun 等待信号，从队列取出数据，通过串口写入蓝牙模块
7. 通过串口发送到蓝牙设备

## 四、注意事项

1. **数据长度限制**:
   - 蓝牙原始数据最大长度: 250字节
   - 网关内部消息最大长度: 253字节 (3字节头部 + 250字节内容)
   - 蓝牙AT指令最大长度: 260字节 (8字节前缀 + 2字节ID + 250字节消息 + 2字节结束符)

2. **线程安全**:
   - 上行数据使用 `up_queue` 和 `up_queue_mutex` 保护
   - 下行数据使用 `write_queue` 和 `write_mutex` 保护
   - 使用条件变量 (`up_queue_cond`, `write_cond`) 进行线程同步

3. **错误处理**:
   - 数据格式错误时，会清空缓冲区并重置FSM状态
   - 超过最大长度限制的数据会被丢弃
   - MQTT发送失败会记录错误日志

4. **性能优化**:
   - 使用线程池处理MQTT发布和串口发送任务
   - 实现回压策略（丢弃、合并、降采样、写入outbox）
   - AT指令有200ms的发送间隔限制
