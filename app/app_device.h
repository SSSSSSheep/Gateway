#ifndef __APP_DEVICE_H__
#define __APP_DEVICE_H__

#include <pthread.h>
#include "app_buffer.h"

// ?υτ????
typedef struct
{
    char *filename;                        // ?????υτ?????
    int fd;                                // ?????????????
    Buffer *up_buffer;                     // ???§έ?????
    Buffer *down_buffer;                   // ???§έ?????
    pthread_t read_thread;                 // ????????????????????
    int is_running;                        // ?υτ???????????
    int (*post_read)(char *data, int len); // ??????????????????? ???????????=> message
    int (*pre_write)(char *data, int len); // §Υ????????????????? (message => ????????)
    long last_write_time;                  // ???§Υ??????????
} Device;

/**
 * @brief ??????υτ
 *
 * @param filename ?????υτ?????
 * @return Device* ?υτ???
 */
Device *app_device_init(char *filename);

/**
 * @brief ?????υτ
 *
 * @return int 0 ?????-1 ???
 */
int app_device_start();

/**
 * @brief ????υτ
 *
 * @return int 0 ?????-1 ???
 */
int app_device_close();

#endif // !__APP_DEVICE_H__